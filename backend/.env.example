DATABASE_URL="mysql://hatamo_user:hatamo_password@localhost:3306/hatamo_db"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
FRONTEND_URL="http://localhost:3000"
ADMIN_BACKEND_URL="http://localhost:5001"
PORT=5000
NODE_ENV="development"

# Email Configuration
# Set to "true" to enable email sending, "false" to use console logging only
ENABLE_EMAIL="false"

# SMTP Configuration (optional - leave empty to use Ethereal test account in development)
# For production, configure your SMTP provider (Gmail, SendGrid, AWS SES, etc.)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="HATAMO <noreply@hatamo.com>"